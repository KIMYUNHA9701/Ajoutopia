<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.crux16_ajou">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".tradeCouponActivity"></activity>
        <activity android:name=".mypageMainActivity" />
        <activity android:name=".writeStudy" />
        <activity android:name=".studyMainActivity" />
        <activity android:name=".writeQnaLecture" />
        <activity android:name=".writeQnaTask" />
        <activity
            android:windowSoftInputMode="stateHidden"
            android:name=".qnaMain2Activity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FirstpageActivity" />
        <activity
            android:windowSoftInputMode="stateHidden"
            android:name=".qnaMainActivity" />
    </application>

</manifest>
